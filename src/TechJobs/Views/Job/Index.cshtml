@model TechJobs.Models.Job
@{
// TODO #2.1- declare the ViewModel (outside of this @{ } block)
}

<table class="job-listing">
    <tr>
        <th>Name</th>
        <th>Employer</th>
        <th>Location</th>
        <th>CoreCompetency</th>
        <th>PositionType</th>
    </tr>

    <tr>
        <th>@Model.Name</th>
        <th>@Model.Employer</th>
        <th>@Model.Location</th>
        <th>@Model.CoreCompetency</th>
        <th>@Model.PositionType</th>
    </tr>


    
    @{ 
// TODO #2.2 - display the job fields, one per row (outside of this @{ } block)
    }
</table>